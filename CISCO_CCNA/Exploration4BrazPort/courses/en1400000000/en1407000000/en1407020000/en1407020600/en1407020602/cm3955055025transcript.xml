<transcript lastedited="12.03.08 12:12:33">
<title><content-text><![CDATA[7.2.6 - Configuring NAT Overload]]></content-text></title>
<brief><content-text><![CDATA[The diagram depicts the steps and command syntax involved in configuring NAT Overload for a pool of public IP addresses. An example configuration is provided.<br><br>Commands:<br><br>Step 1.<br>Action: Define a standard access list permitting those addresses that are to be translated.<br>Command: Router(config)#access-list ACL-number permit source [source-wildcard]<br>Notes: Enter the global command no access-list access-list-number to remove the access list.<br><br>Step 2.<br>Action: Specify the global address to be used for overloading as a pool.<br>Command: Router(config)#i p nat pool name start-i p end-i p {netmask netmask | prefix-length prefix-length}<br><br>Step 3.<br>Action: Establish overload translation.<br>Command: Router {config}#i p nat inside source list ACL-number pool name overload.<br><br>Step 4.<br>Action: Specify the inside interface.<br>Commands:<br>Router(config)#interface type number<br>Router(config-i f)#i p nat inside<br>Notes: Enter the interface command. The C L I prompt changes from (config)# to (config-i f)#.<br><br>Step 5.<br>Action: Specify the outside interface.<br>Commands:<br>Router(config-i f)#interface type number<br>Router(config-i f)#i p nat outside<br><br><br>Example:<br>The diagram is the same as the 7.2.5 example topology. <br><br>Router Output Configuration Commands:<br>access-list 1 permit 192.168.0.0 0.0.255.255<br>Defines which addresses are eligible to be translated.<br>i p nat pool NAT-POOL2 209.165.200.226 209.165.200.240<br>Defines a pool of addresses named NAT-POOL2 to be used in NAT translation.<br>i p nat inside source list 1 pool NAT-POOL2 overload<br>Binds the NAT pool with ACL 1.<br>interface serial0/0/0<br>i p nat inside<br>Identifies interface serial0/0/0 as an inside NAT interface.<br>interface serial0/1/0<br>i p nat outside<br>Identifies interface serial0/1/0 as an outside NAT interface.<br>]]></content-text></brief>
<full><content-text><![CDATA[FULL - NOT FOR PRODUCTION]]></content-text></full>
</transcript>
